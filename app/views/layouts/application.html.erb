<!DOCTYPE html>
<html>
  <head>
    <title>Rails Template</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "company_employee_dashbaord", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  
<!--NAV BAR BELOW-->
  <body>
    <nav>
      <%if !student_signed_in?%>
         <%# <a href="/" class="inline-flex items-center px-4 py-2 rounded-full bg-gradient-to-r from-orange-300 to-orange-0 text-blue-700 font-bold hover:bg-gradient-to-r from-orange-400 to-orange-600">
          Home
        </a> %>
      <%else%>
        <%= link_to "Dashboard", student_dashboard_path(current_student) %>
        <%= link_to "My Profile", my_profile_path(current_student) %>
        
        <a href="/students/sign_out">Sign Out</a>
      <%end%>
    </nav>
    <%= yield %>
  </body>
</html>
